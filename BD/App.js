import React from 'react';
// Importamos la pantalla que cree en la carpeta screens
import InsertUsuarioScreen from './screens/InsertUsuarioScreens';

export default function App() {
  // Renderizamos directamente tu pantalla de práctica
  return (
    <InsertUsuarioScreen />
  );
}